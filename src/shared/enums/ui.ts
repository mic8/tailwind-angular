export enum Color {
    Light = 'light',
    Dark = 'dark',
    Default = 'default',
    Success = 'success',
    Primary = 'primary',
    Danger = 'danger',
    Info = 'info',
    Warning = 'warning',
}

export enum ContainerType {
    Default = 'container',
    Fluid = 'container-fluid',
}

export enum HorizontalPosition {
    Left = 'left',
    Right = 'right',
}

export enum Size {
    ExtraSmall = 'extra-small',
    Small = 'small',
    Medium = 'medium',
    Large = 'large',
    ExtraLarge = 'extra-large',
}

export enum ButtonType {
    Button = 'button',
    Submit = 'submit',
    Reset = 'reset',
}

export enum ButtonLoader {
    BallFall,
    Timer,
}

export enum DialogType {
    Confirmation,
    Error,
    Success,
    Information,
}
